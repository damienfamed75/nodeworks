---
navigation:
  parent: index.md
  title: ItemsHandle
  position: 20
categories:
- lua-api
---

# ItemsHandle

Represents a stack of items matching some filter. Most network operations return an
`ItemsHandle` you can chain further operations on.

TODO: expand this page — currently a stub so the `<LuaCode>` tag's hover refs resolve.
