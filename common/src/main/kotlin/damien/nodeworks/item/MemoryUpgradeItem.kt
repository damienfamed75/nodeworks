package damien.nodeworks.item

import net.minecraft.world.item.Item

/**
 * Memory Upgrade — placed in the Instruction Storage upgrade slot.
 * Each upgrade unlocks 12 additional instruction set slots (up to 2 upgrades = 36 total).
 */
class MemoryUpgradeItem(properties: Properties) : Item(properties)
