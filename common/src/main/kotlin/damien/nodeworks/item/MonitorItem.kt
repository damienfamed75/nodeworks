package damien.nodeworks.item

import net.minecraft.world.item.Item

/**
 * Monitor — attaches to a node face to display an item icon and its network count.
 * Right-click a node face to attach. Left-click to remove.
 */
class MonitorItem(properties: Properties) : Item(properties)
